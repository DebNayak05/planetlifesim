import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false,
  devIndicators: {
    position: "bottom-right", // Change to your desired position
  },
};

export default nextConfig;
